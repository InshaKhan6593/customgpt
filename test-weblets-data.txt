═══════════════════════════════════════════════════════════════════════
  TEST DATA — 3 Weblets with MCPs for Testing Flows (Sequential + Hybrid)
  Use Prisma Studio or seed script to insert. Replace YOUR_USER_ID below.
═══════════════════════════════════════════════════════════════════════

IMPORTANT: Replace these placeholders before inserting:
  - YOUR_USER_ID → your actual user ID from the User table
  - YOUR_GITHUB_PAT → GitHub Settings → Developer Settings → Personal Access Tokens


═══════════════════════════════════════════════════════
  WEBLET 1: "DevResearcher" — Research & Code Analysis Agent
═══════════════════════════════════════════════════════

-- Weblet Record --
INSERT fields:
  id:            "test-weblet-researcher"
  developerId:   "YOUR_USER_ID"
  name:          "DevResearcher"
  slug:          "dev-researcher"
  description:   "Researches codebases, finds documentation, analyzes GitHub repos, and summarizes technical topics using web search and GitHub integration."
  category:      RESEARCH
  isActive:      true
  isPublic:      true
  capabilities:  { "webSearch": true, "imageGeneration": false, "codeInterpreter": false }
  conversationStarters: ["Research the latest Next.js 15 features", "Analyze the top 5 React state management libraries", "Find security best practices for Node.js APIs"]

-- Active Version (v1) --
INSERT fields:
  webletId:    "test-weblet-researcher"
  versionNum:  1
  status:      ACTIVE
  model:       "anthropic/claude-3.5-sonnet"
  prompt:      |
    You are DevResearcher, an expert technical research assistant specializing in software engineering topics.

    Your core strengths:
    - Deep research into programming languages, frameworks, and tools
    - Analyzing GitHub repositories for architecture patterns and code quality
    - Finding and summarizing official documentation
    - Comparing technologies with data-driven analysis
    - Identifying security vulnerabilities and best practices

    When researching:
    1. Start with broad web searches to understand the landscape
    2. Use GitHub tools to examine actual codebases when relevant
    3. Cross-reference multiple sources for accuracy
    4. Always cite your sources with URLs
    5. Present findings in a structured format with clear sections

    Output format:
    - Use markdown headings (##) for major sections
    - Use bullet points for key findings
    - Include code examples when discussing technical concepts
    - Add a "Sources" section at the bottom with links
    - Flag any information that may be outdated or uncertain

-- MCP Server 1: DeepWiki (free, no API key needed) --
INSERT fields:
  webletId:         "test-weblet-researcher"
  serverUrl:        "https://mcp.deepwiki.com/sse"
  label:            "DeepWiki"
  description:      "Read documentation and wiki pages from any GitHub repository. Free, no API key required."
  authType:         NONE
  authToken:        null
  isActive:         true
  requiresUserAuth: false

-- MCP Server 2: GitHub (user provides their own PAT) --
INSERT fields:
  webletId:         "test-weblet-researcher"
  serverUrl:        "https://api.githubcopilot.com/mcp/"
  label:            "GitHub"
  description:      "Access GitHub repos, issues, PRs, and code search. You'll need to provide your own GitHub Personal Access Token."
  authType:         BEARER_TOKEN
  authToken:        null
  isActive:         true
  requiresUserAuth: true


═══════════════════════════════════════════════════════
  WEBLET 2: "CodeCrafter" — Full-Stack Code Generator
═══════════════════════════════════════════════════════

-- Weblet Record --
INSERT fields:
  id:            "test-weblet-coder"
  developerId:   "YOUR_USER_ID"
  name:          "CodeCrafter"
  slug:          "code-crafter"
  description:   "Generates production-ready code for full-stack web apps. Specializes in React, Next.js, TypeScript, and Node.js with best practices built in."
  category:      CODE
  isActive:      true
  isPublic:      true
  capabilities:  { "webSearch": false, "imageGeneration": false, "codeInterpreter": true }
  conversationStarters: ["Build a REST API with Express and Prisma", "Create a React hook for infinite scrolling", "Write a GitHub Actions CI/CD pipeline"]

-- Active Version (v1) --
INSERT fields:
  webletId:    "test-weblet-coder"
  versionNum:  1
  status:      ACTIVE
  model:       "anthropic/claude-3.5-sonnet"
  prompt:      |
    You are CodeCrafter, a senior full-stack developer who writes clean, production-ready code.

    Tech stack expertise:
    - Frontend: React 19, Next.js 15, TypeScript, Tailwind CSS, shadcn/ui
    - Backend: Node.js, Express, Prisma ORM, PostgreSQL
    - DevOps: Docker, GitHub Actions, Vercel, AWS
    - Testing: Vitest, Playwright, React Testing Library

    Coding principles:
    1. Write TypeScript with strict types — no `any` unless absolutely necessary
    2. Follow established conventions of each framework
    3. Handle errors gracefully with proper error boundaries and try/catch
    4. Use meaningful variable/function names that convey intent
    5. Include input validation at system boundaries (API routes, form inputs)
    6. Write secure code — sanitize inputs, use parameterized queries, no hardcoded secrets
    7. Add brief comments only for non-obvious logic

    Output format:
    - Always use fenced code blocks with language specified
    - Provide complete, runnable files — not fragments
    - Include file paths as comments at the top of each code block
    - List any dependencies that need to be installed
    - Add a brief explanation of architectural decisions

-- MCP Server: GitHub (user provides their own PAT) --
INSERT fields:
  webletId:         "test-weblet-coder"
  serverUrl:        "https://api.githubcopilot.com/mcp/"
  label:            "GitHub"
  description:      "Create repos, push code, open PRs, and manage issues directly from the chat. Requires your GitHub PAT."
  authType:         BEARER_TOKEN
  authToken:        null
  isActive:         true
  requiresUserAuth: true


═══════════════════════════════════════════════════════
  WEBLET 3: "ContentWriter" — Professional Content Writer
═══════════════════════════════════════════════════════

-- Weblet Record --
INSERT fields:
  id:            "test-weblet-writer"
  developerId:   "YOUR_USER_ID"
  name:          "ContentWriter"
  slug:          "content-writer"
  description:   "Writes polished blog posts, documentation, technical articles, and marketing copy. Uses web search to fact-check and enrich content with current data."
  category:      WRITING
  isActive:      true
  isPublic:      true
  capabilities:  { "webSearch": true, "imageGeneration": true, "codeInterpreter": false }
  conversationStarters: ["Write a blog post about AI agents in 2026", "Create API documentation for a REST endpoint", "Draft a landing page copy for a SaaS product"]

-- Active Version (v1) --
INSERT fields:
  webletId:    "test-weblet-writer"
  versionNum:  1
  status:      ACTIVE
  model:       "anthropic/claude-3.5-sonnet"
  prompt:      |
    You are ContentWriter, a professional content creator who produces publication-ready written content.

    Writing specializations:
    - Technical blog posts and tutorials
    - API and developer documentation
    - Marketing copy (landing pages, emails, social media)
    - Whitepapers and research summaries
    - Product announcements and changelogs

    Writing process:
    1. Understand the target audience and purpose before writing
    2. Research current data and trends using web search to ground your content in facts
    3. Create a clear outline with logical flow before drafting
    4. Write with a confident, engaging tone — avoid filler and fluff
    5. Use concrete examples, statistics, and real-world references
    6. Generate images when visual content would enhance the piece

    Style guidelines:
    - Open with a compelling hook that draws the reader in
    - Use short paragraphs (2-3 sentences max) for readability
    - Include subheadings every 2-3 paragraphs for scanability
    - Bold key terms and important takeaways
    - End with a clear call to action or conclusion
    - When including code, always use fenced code blocks with syntax highlighting

-- MCP Server: Exa Search (free, no API key needed) --
INSERT fields:
  webletId:         "test-weblet-writer"
  serverUrl:        "https://mcp.exa.ai/mcp"
  label:            "Exa Search"
  description:      "AI-powered web search for finding current data, articles, and facts to enrich written content. Free, no API key required."
  authType:         NONE
  authToken:        null
  isActive:         true
  requiresUserAuth: false


═══════════════════════════════════════════════════════════════════════
  TEST FLOW 1: SEQUENTIAL — "Research → Write → Review" Pipeline
═══════════════════════════════════════════════════════════════════════

This flow demonstrates a classic content pipeline:
  Step 1: DevResearcher gathers facts and data
  Step 2: ContentWriter drafts a polished article using the research
  Step 3: DevResearcher reviews the article for technical accuracy

-- UserFlow Record --
INSERT fields:
  id:             "test-flow-sequential"
  userId:         "YOUR_USER_ID"
  name:           "Research → Write → Review"
  description:    "Three-stage content pipeline: research a topic, write an article, then review for accuracy."
  mode:           SEQUENTIAL
  masterWebletId: null
  isPublic:       false
  defaultPrompt:  "Write a comprehensive blog post about building AI agents with the Vercel AI SDK in 2026"
  steps:          [
    {
      "webletId": "test-weblet-researcher",
      "order": 0,
      "role": "Researcher",
      "stepPrompt": "Research this topic thoroughly. Find current stats, official docs, real code examples, and competing approaches. Structure your findings so a writer can easily use them.",
      "inputMapping": "original",
      "hitlGate": false
    },
    {
      "webletId": "test-weblet-writer",
      "order": 1,
      "role": "Writer",
      "stepPrompt": "Using the research provided, write a 1500-word blog post. Include code examples, real statistics, and a clear narrative arc. Target audience: intermediate developers.",
      "inputMapping": "previous",
      "hitlGate": false
    },
    {
      "webletId": "test-weblet-researcher",
      "order": 2,
      "role": "Reviewer",
      "stepPrompt": "Review this blog post for technical accuracy. Check all code examples, verify statistics, and flag any claims that seem incorrect or outdated. Provide the corrected final version.",
      "inputMapping": "previous",
      "hitlGate": true
    }
  ]

Test prompt: "Write a comprehensive blog post about building AI agents with the Vercel AI SDK in 2026"


═══════════════════════════════════════════════════════════════════════
  TEST FLOW 2: HYBRID — "Tech Lead Coordinator" with Specialist Team
═══════════════════════════════════════════════════════════════════════

This flow demonstrates hybrid orchestration:
  Master: DevResearcher (coordinator — decides which agents to call and when)
  Team: CodeCrafter (implements code), ContentWriter (writes docs)

The coordinator dynamically decides the approach based on the user's request.

-- UserFlow Record --
INSERT fields:
  id:             "test-flow-hybrid"
  userId:         "YOUR_USER_ID"
  name:           "Tech Lead Coordinator"
  description:    "A senior tech lead coordinates a coder and writer to deliver complete features with code + documentation."
  mode:           HYBRID
  masterWebletId: "test-weblet-researcher"
  isPublic:       false
  defaultPrompt:  "Build a rate limiter middleware for Express.js with Redis, including full API docs and a blog post explaining the implementation"
  steps:          [
    {
      "webletId": "test-weblet-coder",
      "order": 0,
      "role": "Coder",
      "stepPrompt": "Implement production-ready code. Include types, error handling, tests, and package.json dependencies.",
      "inputMapping": "original",
      "hitlGate": false
    },
    {
      "webletId": "test-weblet-writer",
      "order": 1,
      "role": "Writer",
      "stepPrompt": "Write clear documentation and/or blog content. Reference the actual code implementations when relevant.",
      "inputMapping": "original",
      "hitlGate": false
    }
  ]

Test prompt: "Build a rate limiter middleware for Express.js with Redis, including full API docs and a blog post explaining the implementation"


═══════════════════════════════════════════════════════════════════════
  TEST FLOW 3: SEQUENTIAL with HITL — "Code Review Pipeline"
═══════════════════════════════════════════════════════════════════════

This flow tests human-in-the-loop:
  Step 1: CodeCrafter writes the code
  Step 2: Human reviews (HITL gate) — can approve, reject, or give feedback
  Step 3: ContentWriter writes documentation for the approved code

-- UserFlow Record --
INSERT fields:
  id:             "test-flow-hitl"
  userId:         "YOUR_USER_ID"
  name:           "Code → Review → Document"
  description:    "Write code, get human approval, then auto-generate documentation."
  mode:           SEQUENTIAL
  masterWebletId: null
  isPublic:       false
  defaultPrompt:  "Create a reusable React hook called useDebounce that supports cancellation and immediate execution"
  steps:          [
    {
      "webletId": "test-weblet-coder",
      "order": 0,
      "role": "Coder",
      "stepPrompt": "Write a complete, tested implementation. Include TypeScript types, JSDoc comments, and a usage example.",
      "inputMapping": "original",
      "hitlGate": true
    },
    {
      "webletId": "test-weblet-writer",
      "order": 1,
      "role": "Writer",
      "stepPrompt": "Write developer documentation for this code. Include: overview, API reference, usage examples, edge cases, and a migration guide if replacing an existing solution.",
      "inputMapping": "previous",
      "hitlGate": false
    }
  ]

Test prompt: "Create a reusable React hook called useDebounce that supports cancellation and immediate execution"


═══════════════════════════════════════════════════════════════════════
  COMPOSITION TEST — DevResearcher calls ContentWriter as a child
═══════════════════════════════════════════════════════════════════════

To test composability, create this WebletComposition record:

INSERT fields:
  parentWebletId: "test-weblet-researcher"
  childWebletId:  "test-weblet-writer"

This means when you chat with DevResearcher directly, it can call
ContentWriter as a tool (weblet_content_writer) to draft content
based on its research findings — all within a single chat session.


═══════════════════════════════════════════════════════════════════════
  QUICK SEED SCRIPT (run in Prisma Studio or as a Node script)
═══════════════════════════════════════════════════════════════════════

// Paste into: node -e "..." or create a seed.ts file

const { PrismaClient } = require('@prisma/client')
const prisma = new PrismaClient()

const USER_ID = 'YOUR_USER_ID'  // <-- Replace this

async function seed() {
  // 1. Create Weblets
  const researcher = await prisma.weblet.upsert({
    where: { slug: 'dev-researcher' },
    update: {},
    create: {
      id: 'test-weblet-researcher',
      developerId: USER_ID,
      name: 'DevResearcher',
      slug: 'dev-researcher',
      description: 'Researches codebases, finds documentation, analyzes GitHub repos, and summarizes technical topics.',
      category: 'RESEARCH',
      isActive: true,
      isPublic: true,
      capabilities: { webSearch: true, imageGeneration: false, codeInterpreter: false },
      conversationStarters: [
        'Research the latest Next.js 15 features',
        'Analyze the top 5 React state management libraries',
        'Find security best practices for Node.js APIs'
      ],
    }
  })

  const coder = await prisma.weblet.upsert({
    where: { slug: 'code-crafter' },
    update: {},
    create: {
      id: 'test-weblet-coder',
      developerId: USER_ID,
      name: 'CodeCrafter',
      slug: 'code-crafter',
      description: 'Generates production-ready code for full-stack web apps with React, Next.js, TypeScript, and Node.js.',
      category: 'CODE',
      isActive: true,
      isPublic: true,
      capabilities: { webSearch: false, imageGeneration: false, codeInterpreter: true },
      conversationStarters: [
        'Build a REST API with Express and Prisma',
        'Create a React hook for infinite scrolling',
        'Write a GitHub Actions CI/CD pipeline'
      ],
    }
  })

  const writer = await prisma.weblet.upsert({
    where: { slug: 'content-writer' },
    update: {},
    create: {
      id: 'test-weblet-writer',
      developerId: USER_ID,
      name: 'ContentWriter',
      slug: 'content-writer',
      description: 'Writes polished blog posts, documentation, technical articles, and marketing copy.',
      category: 'WRITING',
      isActive: true,
      isPublic: true,
      capabilities: { webSearch: true, imageGeneration: true, codeInterpreter: false },
      conversationStarters: [
        'Write a blog post about AI agents in 2026',
        'Create API documentation for a REST endpoint',
        'Draft a landing page copy for a SaaS product'
      ],
    }
  })

  // 2. Create Active Versions
  await prisma.webletVersion.upsert({
    where: { webletId_versionNum: { webletId: researcher.id, versionNum: 1 } },
    update: {},
    create: {
      webletId: researcher.id,
      versionNum: 1,
      status: 'ACTIVE',
      model: 'anthropic/claude-3.5-sonnet',
      prompt: `You are DevResearcher, an expert technical research assistant specializing in software engineering topics.

Your core strengths:
- Deep research into programming languages, frameworks, and tools
- Analyzing GitHub repositories for architecture patterns and code quality
- Finding and summarizing official documentation
- Comparing technologies with data-driven analysis
- Identifying security vulnerabilities and best practices

When researching:
1. Start with broad web searches to understand the landscape
2. Use GitHub tools to examine actual codebases when relevant
3. Cross-reference multiple sources for accuracy
4. Always cite your sources with URLs
5. Present findings in a structured format with clear sections

Output format:
- Use markdown headings (##) for major sections
- Use bullet points for key findings
- Include code examples when discussing technical concepts
- Add a "Sources" section at the bottom with links
- Flag any information that may be outdated or uncertain`,
    }
  })

  await prisma.webletVersion.upsert({
    where: { webletId_versionNum: { webletId: coder.id, versionNum: 1 } },
    update: {},
    create: {
      webletId: coder.id,
      versionNum: 1,
      status: 'ACTIVE',
      model: 'anthropic/claude-3.5-sonnet',
      prompt: `You are CodeCrafter, a senior full-stack developer who writes clean, production-ready code.

Tech stack expertise:
- Frontend: React 19, Next.js 15, TypeScript, Tailwind CSS, shadcn/ui
- Backend: Node.js, Express, Prisma ORM, PostgreSQL
- DevOps: Docker, GitHub Actions, Vercel, AWS
- Testing: Vitest, Playwright, React Testing Library

Coding principles:
1. Write TypeScript with strict types — no any unless absolutely necessary
2. Follow established conventions of each framework
3. Handle errors gracefully with proper error boundaries and try/catch
4. Use meaningful variable/function names that convey intent
5. Include input validation at system boundaries
6. Write secure code — sanitize inputs, use parameterized queries, no hardcoded secrets
7. Add brief comments only for non-obvious logic

Output format:
- Always use fenced code blocks with language specified
- Provide complete, runnable files — not fragments
- Include file paths as comments at the top of each code block
- List any dependencies that need to be installed
- Add a brief explanation of architectural decisions`,
    }
  })

  await prisma.webletVersion.upsert({
    where: { webletId_versionNum: { webletId: writer.id, versionNum: 1 } },
    update: {},
    create: {
      webletId: writer.id,
      versionNum: 1,
      status: 'ACTIVE',
      model: 'anthropic/claude-3.5-sonnet',
      prompt: `You are ContentWriter, a professional content creator who produces publication-ready written content.

Writing specializations:
- Technical blog posts and tutorials
- API and developer documentation
- Marketing copy (landing pages, emails, social media)
- Whitepapers and research summaries
- Product announcements and changelogs

Writing process:
1. Understand the target audience and purpose before writing
2. Research current data and trends using web search to ground your content in facts
3. Create a clear outline with logical flow before drafting
4. Write with a confident, engaging tone — avoid filler and fluff
5. Use concrete examples, statistics, and real-world references
6. Generate images when visual content would enhance the piece

Style guidelines:
- Open with a compelling hook that draws the reader in
- Use short paragraphs (2-3 sentences max) for readability
- Include subheadings every 2-3 paragraphs for scanability
- Bold key terms and important takeaways
- End with a clear call to action or conclusion
- When including code, always use fenced code blocks`,
    }
  })

  // 3. Create MCP Servers
  // DeepWiki for DevResearcher (free, no API key)
  await prisma.webletMCPServer.upsert({
    where: { webletId_serverUrl: { webletId: researcher.id, serverUrl: 'https://mcp.deepwiki.com/sse' } },
    update: {},
    create: {
      webletId: researcher.id,
      serverUrl: 'https://mcp.deepwiki.com/sse',
      label: 'DeepWiki',
      description: 'Read documentation and wiki pages from any GitHub repository. Free, no API key required.',
      authType: 'NONE',
      authToken: null,
      isActive: true,
      requiresUserAuth: false,
    }
  })

  // GitHub for DevResearcher (user provides PAT)
  await prisma.webletMCPServer.upsert({
    where: { webletId_serverUrl: { webletId: researcher.id, serverUrl: 'https://api.githubcopilot.com/mcp/' } },
    update: {},
    create: {
      webletId: researcher.id,
      serverUrl: 'https://api.githubcopilot.com/mcp/',
      label: 'GitHub',
      description: 'Access GitHub repos, issues, PRs, and code search. Requires your GitHub PAT.',
      authType: 'BEARER_TOKEN',
      authToken: null,
      isActive: true,
      requiresUserAuth: true,
    }
  })

  // GitHub for CodeCrafter (user provides PAT)
  await prisma.webletMCPServer.upsert({
    where: { webletId_serverUrl: { webletId: coder.id, serverUrl: 'https://api.githubcopilot.com/mcp/' } },
    update: {},
    create: {
      webletId: coder.id,
      serverUrl: 'https://api.githubcopilot.com/mcp/',
      label: 'GitHub',
      description: 'Create repos, push code, open PRs, and manage issues. Requires your GitHub PAT.',
      authType: 'BEARER_TOKEN',
      authToken: null,
      isActive: true,
      requiresUserAuth: true,
    }
  })

  // Exa Search for ContentWriter (free, no API key)
  await prisma.webletMCPServer.upsert({
    where: { webletId_serverUrl: { webletId: writer.id, serverUrl: 'https://mcp.exa.ai/mcp' } },
    update: {},
    create: {
      webletId: writer.id,
      serverUrl: 'https://mcp.exa.ai/mcp',
      label: 'Exa Search',
      description: 'AI-powered web search for finding current data, articles, and facts to enrich written content. Free, no API key required.',
      authType: 'NONE',
      authToken: null,
      isActive: true,
      requiresUserAuth: false,
    }
  })

  // 4. Create Composition (DevResearcher can call ContentWriter)
  await prisma.webletComposition.upsert({
    where: { parentWebletId_childWebletId: { parentWebletId: researcher.id, childWebletId: writer.id } },
    update: {},
    create: {
      parentWebletId: researcher.id,
      childWebletId: writer.id,
    }
  })

  // 5. Create Test Flows
  // Sequential: Research → Write → Review
  await prisma.userFlow.upsert({
    where: { id: 'test-flow-sequential' },
    update: {},
    create: {
      id: 'test-flow-sequential',
      userId: USER_ID,
      name: 'Research → Write → Review',
      description: 'Three-stage content pipeline: research a topic, write an article, then review for accuracy.',
      mode: 'SEQUENTIAL',
      defaultPrompt: 'Write a comprehensive blog post about building AI agents with the Vercel AI SDK in 2026',
      steps: [
        { webletId: researcher.id, order: 0, role: 'Researcher', stepPrompt: 'Research this topic thoroughly. Find current stats, official docs, real code examples, and competing approaches.', inputMapping: 'original', hitlGate: false },
        { webletId: writer.id, order: 1, role: 'Writer', stepPrompt: 'Using the research provided, write a 1500-word blog post with code examples and real statistics. Target: intermediate developers.', inputMapping: 'previous', hitlGate: false },
        { webletId: researcher.id, order: 2, role: 'Reviewer', stepPrompt: 'Review this blog post for technical accuracy. Verify code examples and statistics. Provide the corrected final version.', inputMapping: 'previous', hitlGate: true },
      ],
    }
  })

  // Hybrid: Tech Lead Coordinator
  await prisma.userFlow.upsert({
    where: { id: 'test-flow-hybrid' },
    update: {},
    create: {
      id: 'test-flow-hybrid',
      userId: USER_ID,
      name: 'Tech Lead Coordinator',
      description: 'A senior tech lead coordinates a coder and writer to deliver complete features with code + documentation.',
      mode: 'HYBRID',
      masterWebletId: researcher.id,
      defaultPrompt: 'Build a rate limiter middleware for Express.js with Redis, including full API docs and a blog post',
      steps: [
        { webletId: coder.id, order: 0, role: 'Coder', stepPrompt: 'Implement production-ready code with types, error handling, tests, and dependencies.', inputMapping: 'original', hitlGate: false },
        { webletId: writer.id, order: 1, role: 'Writer', stepPrompt: 'Write clear documentation and/or blog content. Reference actual code implementations.', inputMapping: 'original', hitlGate: false },
      ],
    }
  })

  // Sequential with HITL: Code → Review → Document
  await prisma.userFlow.upsert({
    where: { id: 'test-flow-hitl' },
    update: {},
    create: {
      id: 'test-flow-hitl',
      userId: USER_ID,
      name: 'Code → Review → Document',
      description: 'Write code, get human approval, then auto-generate documentation.',
      mode: 'SEQUENTIAL',
      defaultPrompt: 'Create a reusable React hook called useDebounce that supports cancellation and immediate execution',
      steps: [
        { webletId: coder.id, order: 0, role: 'Coder', stepPrompt: 'Write a complete, tested implementation with TypeScript types, JSDoc comments, and a usage example.', inputMapping: 'original', hitlGate: true },
        { webletId: writer.id, order: 1, role: 'Writer', stepPrompt: 'Write developer documentation: overview, API reference, usage examples, edge cases, and migration guide.', inputMapping: 'previous', hitlGate: false },
      ],
    }
  })

  console.log('✅ Seeded 3 weblets, 3 versions, 4 MCP servers, 1 composition, 3 flows')
}

seed().catch(console.error).finally(() => prisma.$disconnect())
